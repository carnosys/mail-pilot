# MailPilot

MailPilot is a Django-based email client project with inbox, sent mail, archive, login, registration, and compose flows.

## Run locally

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```
